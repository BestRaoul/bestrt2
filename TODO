threading plan + execute it
	single thread simplicity
	quick switch to multiple

mouse images
	default
	4 arrows move
	2 arrows rot/scale around axis

fix issue with total_move and anchor -> after using shift absulute center is not there

item: 
	+select, +modif
	?color picker
	shift + XZY



1: do fake 3d
 FIX draw_line Z linear shift but it aint always that
2. do actuall render bs
 add bonuses (lights, specular, ..)
3. do fake generation
	shift + _p
	save to disk
	use motions (frames, time_duration) -> ffmpeg -> ntfy
4. do threading

----- Things i dont want to do, but have to be done

//make add, move the pointers so that add_item does not break previous add_motions
//remove mallocs for static size arrays
//rotate default cylinder
//linux -> CAMERA MOVEMENT - mouse
//cam add CAMERA FLIPP



//PBR scatter
- albedo
- specular
- metalic
- transmission
- emission
- normal (bump to normal conversion)

----------- NOW ---------------
OK -1. normal perturbation
?fov (-.5 ZOOM}
0. sphere UV rotation
1. point lights
2. cylinder + 2nd degree object
3. object selection
4. showoff scenes (materials)
5. UI (material debug, vec3's, color materials);
-----------AT 42---------------
1. parsing
2. no crash
-----------NEVER---------------
IGNORED - 3. window resizing

//TODO
1. light sampling / important for point light and noise reduction
2. focus blur
3. ntfy
	//cloudflare ->cronjob -> always notify
3.1 better tweening
	//add_tween()
	//from, to (double, vec3)
	//duration
	//ease_method
	//loop, at exit, ...
4. lights
	// fix last_bounce
	// check if color spots work
	// point lights
	// ambient light
	// diffuse light
5. tris
	//read from obj
11. instance translation with matrix
99. aabb