threading plan + execute it
	single thread simplicity
	quick switch to multiple

mouse images
	default
	4 arrows move
	2 arrows rot/scale around axis

fix issue with total_move and anchor -> after using shift absulute center is not there

item: 
	+select, +modif
	?color picker
	shift + XZY



1: do fake 3d
 FIX draw_line Z linear shift but it aint always that
2. do actuall render bs
 add bonuses (lights, specular, ..)
3. do fake generation
	shift + _p
	save to disk
	use motions (frames, time_duration) -> ffmpeg -> ntfy
4. do threading

----- Things i dont want to do, but have to be done

//make add, move the pointers so that add_item does not break previous add_motions
//remove mallocs for static size arrays
//rotate default cylinder
//linux -> CAMERA MOVEMENT - mouse
//cam add CAMERA FLIPP

NdotV inside scatter specular reflection???

fix ambient bounces


----------- NOW ---------------
0. cylinder + 2nd degree object
2. proper 8-bit read
3. showoff scenes 6-7-8-9
4. object selection
97. (check sphere UV rotation)
96. light flicker spinning quad + acne on tilted box
-----------AT 42---------------
0. normal map are fugged
1. parsing
2. no crash
3. ptrs ALLL
--- after
//98. (bump to normal conversion)
//6. add_item, add_tween (duration, loop, delay)
3. tween, item, lights, sky, rendermovie CLEANUP
4. UI (material debug, vec3's, color materials)
5. plenty render modes
6. ao
-----------NEVER---------------
IGNORED - 2. directional light/IBL (roughness)
IGNORED - 3. window resizing
IGNORED - 0. ZOOM
IGNORED - 7. proper hdri map to cube idk
IGNORED - ?. specular TINT

//TODO
2. focus blur
3. ntfy
	//cloudflare ->cronjob -> always notify
3.1 better tweening
	//add_tween()
	//from, to (double, vec3)
	//duration
	//ease_method
	//loop, at exit, ...
5. tris
	//read from obj
11. instance translation with matrix
22. MIS
33. shaders 
44. bidiractional path tracing
99. aabb